# StaffReports

Discord bot for requested and staff-created reports.

## Start
1. Install dependencies with `npm install`.
2. Add `DISCORD_TOKEN` to your hosting environment variables.
3. Run `npm start`.
